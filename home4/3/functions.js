'use strict';

/**
 * Выбор случайного числа от 0 до max
 * @param {Number} max - максимальное значение
 * @returns {Number}
 */
function getRandomInt(max) {
    return Math.floor(Math.random() * Math.floor(max));
}